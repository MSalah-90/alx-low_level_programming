#include <stdio.h>
#include "main.h"

/**
 * mul -  Entry - point
 *
 * @a: is the input num one
 * @b: is the input num two
 *
 * Describtion: mul returns product of two num
 *
 * Return: product
*/

int mul(int a, int b)
{
		return (a * b);
}
